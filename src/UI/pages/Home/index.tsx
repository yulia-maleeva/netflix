import React, { FC } from "react";

import MovieCard from "../../../components/organisms/MovieCard";

const Home: FC = () => (
  <div className="w-[200px] h-[300px] bg-white">
    <MovieCard title="Title" poster="" />
  </div>
);

export default Home;
